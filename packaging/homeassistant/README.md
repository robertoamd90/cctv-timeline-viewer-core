# CCTV Viewer

CCTV Viewer indexes recordings stored in Home Assistant Media and displays all
cameras on a synchronized timeline. Sources are mounted read-only and the local
index is stored in the app data directory.

After installation, start the app and enable **Show in sidebar**. The app is
available only to Home Assistant administrators.
